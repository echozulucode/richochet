# teams-styled-bold

**Source:** hand-written (`source.html`), 2026-09-10. **Not yet captured from real Teams** — replace with a real capture via `just capture teams-styled-bold` during the Phase 1 spike.

**What this proves:** The exact example from docs/plan.md. Presentational markup must normalize to intent, and the whitespace must be hoisted OUT of the mark or the Markdown renders as `** Important **`.

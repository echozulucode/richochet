# teams-monospace-span

**Source:** hand-written (`source.html`), 2026-09-10. **Not yet captured from real Teams** — replace with a real capture via `just capture teams-monospace-span` during the Phase 1 spike.

**What this proves:** Teams may express inline code as a monospace font-family rather than a <code> tag.

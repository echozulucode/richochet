# teams-pre-code

**Source:** hand-written (`source.html`), 2026-09-10. **Not yet captured from real Teams** — replace with a real capture via `just capture teams-pre-code` during the Phase 1 spike.

**What this proves:** Code block with a language class. Whitespace inside <pre> must NOT be collapsed.

# teams-unsafe

**Source:** hand-written (`source.html`), 2026-09-10. **Not yet captured from real Teams** — replace with a real capture via `just capture teams-unsafe` during the Phase 1 spike.

**What this proves:** Clipboard HTML is untrusted input from another process. Scripts and handlers must be stripped.

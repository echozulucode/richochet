# teams-whitespace-collapse

**Source:** hand-written (`source.html`), 2026-09-10. **Not yet captured from real Teams** — replace with a real capture via `just capture teams-whitespace-collapse` during the Phase 1 spike.

**What this proves:** Outside <pre>, runs of whitespace collapse to one space per the HTML rules.
